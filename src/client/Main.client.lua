--!strict
-- Main.client.lua
-- Client entry point. Scripts are also auto-run since they have .client suffix,
-- but this gives a single place to print a boot message.

print("[Client] Stretch Your Limbs Simulator loaded.")
print("[Client] Climb the tree, then press SPACE or E from the top branch to glide!")
